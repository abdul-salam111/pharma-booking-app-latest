import 'package:flutter/material.dart';

// Alignment
const center = Alignment.center;
const centerLeft = Alignment.centerLeft;
const centerRight = Alignment.centerRight;
const topCenter = Alignment.topCenter;
const topLeft = Alignment.topLeft;
const topRight = Alignment.topRight;
const bottomCenter = Alignment.bottomCenter;
const bottomLeft = Alignment.bottomLeft;
const bottomRight = Alignment.bottomRight;

// Custom common alignments
const topLeftQuarter = Alignment(-0.75, -0.75);
const topRightQuarter = Alignment(0.75, -0.75);
const bottomLeftQuarter = Alignment(-0.75, 0.75);
const bottomRightQuarter = Alignment(0.75, 0.75);
const slightlyAboveCenter = Alignment(0.0, -0.3);
const slightlyBelowCenter = Alignment(0.0, 0.3);

// MainAxisAlignment
const mainAxisStart = MainAxisAlignment.start;
const mainAxisCenter = MainAxisAlignment.center;
const mainAxisEnd = MainAxisAlignment.end;
const mainAxisSpaceBetween = MainAxisAlignment.spaceBetween;
const mainAxisSpaceAround = MainAxisAlignment.spaceAround;
const mainAxisSpaceEvenly = MainAxisAlignment.spaceEvenly;

// CrossAxisAlignment
const crossAxisStart = CrossAxisAlignment.start;
const crossAxisCenter = CrossAxisAlignment.center;
const crossAxisEnd = CrossAxisAlignment.end;
const crossAxisStretch = CrossAxisAlignment.stretch;
const crossAxisBaseline = CrossAxisAlignment.baseline;

// TextAlign
const textAlignLeft = TextAlign.left;
const textAlignRight = TextAlign.right;
const textAlignCenter = TextAlign.center;
const textAlignJustify = TextAlign.justify;
const textAlignStart = TextAlign.start;
const textAlignEnd = TextAlign.end;
